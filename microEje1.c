#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

    unsigned char operation[] = "2+(5*3)+(2/3)-1";
    int i, j;
    char tam = sizeof(operation);
    char pila[tam];
    char postFixed[tam];

    for (i = 0; i <= (strlen(operation) - 1); i++) {
        pila[i] = 0;
        printf("Los numeros son %d\n", pila[i]);

        if (pila[0] == 0 && ) 

       

     
        
    }


       
    return 0;
}